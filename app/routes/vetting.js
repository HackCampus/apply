const limitToMatchers = require('../middlewares/limitToMatchers')

module.exports = models => {
  const {User} = models

  function routes (app) {
    app.get('/applications', )
  }

  return {
    routes,
  }
}
